<!-- Validation failure messages -->
<% if @med_type.present? %>
  <% if @med_type.errors.any? %>
    <% @med_type.errors.full_messages.each do |message| %>
      <div class="alert alert-danger">
        <%= message %>
      </div>
    <% end %>
  <% end %>
<% end %>

<h1>
  Add a new med type
</h1>


<hr>

<!-- Form med_type start -->
<form action="/create_med_type" method="post">
<!-- Form med_type end -->

  <!-- Label and input for name start -->
  <div class="form-group">
    <label for="name">
      Name
    </label>

    <!-- Input for name start -->
    <input type="text" id="name" name="name" class="form-control" value="<%= @med_type.name %>">
    <!-- Input for name end -->
  </div>
  <!-- Label and input for name end -->

  <button class="btn btn-block btn-outline-secondary">
    Create med type
  </button>
</form>

<hr>

<a href="/med_types">
  Go back
</a>
